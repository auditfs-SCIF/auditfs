// Partie 1 — Calcul SHA-256 en streaming
// TODO: implémenter le calcul SHA-256 pour les fichiers
// Utiliser la crate sha2

pub fn hash_file(_path: &str) -> anyhow::Result<String> {
    Ok("0".repeat(64)) // chaîne de 64 caractères
}
